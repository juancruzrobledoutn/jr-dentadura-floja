# FastAPI and ASGI server
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
alembic==1.15.1
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6

# Redis for pub/sub and cache
redis==5.2.1

# Authentication
PyJWT==2.10.1

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.1.0

# HTTP client (for Ollama and external APIs)
httpx==0.28.1

# Password hashing (for production)
passlib[bcrypt]==1.7.4

# Environment variables
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# TOTP 2FA
pyotp==2.9.0

# Encryption at rest (TOTP secrets, etc.) — required by S1.1.A
cryptography>=42.0,<46.0

# Development tools
pytest==8.3.4
pytest-asyncio==0.25.2
watchfiles>=1.0.0  # Required for reliable hot-reload on Windows
